# Secrets — never commit
.env
devices.json
tinytuya.json

# Webapp secrets + per-machine generated artifacts (public repo!)
config/webapp_config.json
config/display_names.json
config/tuya_display_names.json
config/location.json
config/tariff.json
config/pv_system.json
webapp/certificates/
webapp/*.log
# Energy-history store (server-side SQLite; per-machine runtime data)
webapp/*.sqlite3
webapp/*.sqlite3-*
app/webapp/static/ca.crt
app/webapp/static/*.mobileconfig

# Pasted-image / scratch temp dir (e.g. screenshots dropped into chat) —
# never commit (public repo).
.launcher-tmp/

# Virtual env
.venv/
venv/

# Python
__pycache__/
*.pyc
*.py[cod]
*.egg-info/
.pytest_cache/

# Streamlit
.streamlit/secrets.toml

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
