# Test-only dependencies. Install with:
#   .venv\Scripts\python.exe -m pip install -r requirements-dev.txt

pytest>=8.0

# Backend smoke layer (tests/api): FastAPI TestClient drives the real app
# in-process. Starlette's TestClient is built on httpx.
httpx>=0.27

# Playwright browser-E2E suite (tests/e2e). After install, fetch browsers:
#   .venv\Scripts\python.exe -m playwright install chromium webkit
pytest-playwright>=0.5

# Voice Tier-2 model benchmark (scripts/voice_bench, issue #234): reads the
# utterance dataset (YAML) and validates model output against the intent schema.
pyyaml>=6.0
jsonschema>=4.0
